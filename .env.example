# required for admin API access (`x-admin-token` header)
ADMIN_TOKEN=change-me

# SQLite database file used by the SvelteKit API routes
SQLITE_PATH=./data/sports.sqlite

# Optional: set only if API is hosted on another origin
VITE_API_BASE_URL=

# Optional: CORS allow-origin for cross-origin API calls
CORS_ORIGIN=
